name: nexus_printer
description: Receipt layout, ESC/POS encoding and the Bluetooth printer transport.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  nexus_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
