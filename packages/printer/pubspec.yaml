name: nexus_printer
description: Receipt layout, ESC/POS encoding and the Bluetooth printer transport.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  nexus_core:
    path: ../core
  permission_handler: ^13.0.2
  print_bluetooth_thermal: ^1.2.4
  shared_preferences: ^2.5.5

dev_dependencies:
  fake_async: ^1.3.3
  flutter_test:
    sdk: flutter
  shared_preferences_platform_interface: ^2.4.2
