# End-to-end scenarios

Owner: QA. Scenario tests run against the Firebase emulator and are written from the contracts in `docs/`, not from the implementation.

`tool/check.sh` skips this package. Run it with `tool/check.sh --e2e` once the emulator is running (C-4).
