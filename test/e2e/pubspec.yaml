name: nexus_e2e
description: Scenario tests against the Firebase emulator. Written from the contracts in docs/.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  nexus_core:
    path: ../../packages/core

dev_dependencies:
  test: ^1.26.0
